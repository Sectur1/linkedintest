import React from "react";

const NetworkNode1 = (children: any) => {
    console.log(children)
    return (
        <React.Fragment>
            <h1>This is the Parent</h1>
        </React.Fragment>
    );
}

export default NetworkNode1